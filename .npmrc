# Keep the workspace strict but allow apps to hoist their own runtime deps.
link-workspace-packages=true
prefer-workspace-packages=true
# Remotion + native binaries need real node_modules layout.
node-linker=hoisted
